<form method="post" action="{{ path('app_postulation_delete', {'annoncePostulation': postulation.annoncePostulation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ postulation.annoncePostulation) }}">
    <button class="btn">Delete</button>
</form>
