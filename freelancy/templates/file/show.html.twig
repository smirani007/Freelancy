{% extends 'base.html.twig' %}

{% block title %}File{% endblock %}

{% block body %}
    <h1>File</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdFile</th>
                <td>{{ file.idFile }}</td>
            </tr>
            <tr>
                <th>Cv</th>
                <td>{{ file.cv }}</td>
            </tr>
            <tr>
                <th>Deplome</th>
                <td>{{ file.deplome }}</td>
            </tr>
            <tr>
                <th>Lettermotivation</th>
                <td>{{ file.lettermotivation }}</td>
            </tr>
            <tr>
                <th>Namecv</th>
                <td>{{ file.namecv }}</td>
            </tr>
            <tr>
                <th>Namedeplome</th>
                <td>{{ file.namedeplome }}</td>
            </tr>
            <tr>
                <th>Namemotivation</th>
                <td>{{ file.namemotivation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_file_index') }}">back to list</a>

    <a href="{{ path('app_file_edit', {'idFile': file.idFile}) }}">edit</a>

    {{ include('file/_delete_form.html.twig') }}
{% endblock %}
