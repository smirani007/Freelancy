<form method="post" action="{{ path('app_file_delete', {'idFile': file.idFile}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ file.idFile) }}">
    <button class="btn">Delete</button>
</form>
